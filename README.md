## Templates
Gulp + Browser-Sync + AngularJs + SASS + Bootstrap

## Requeriments
1. Gulp global

```bash
    $ npm install gulp -g
```

2. Browser-Sync global 

```bash
    $ npm install browser-sync -g
```

## Configuration

```bash
    $ npm install
```
## Start 

```bash
    $ gulp
```
