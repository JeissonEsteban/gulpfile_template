# gulpfile_template
Gulp + Browser-Sync + AngularJs + SASS + Bootstrap

# requeriments
1. Gulp global
...
    $ npm install gulp -g
...
2. Browser-Sync global 
...
    $ npm install browser-sync -g
...

# configuration

...
    $ npm install
...

# start 

...
    $ gulp
...